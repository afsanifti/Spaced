name: Build & Release Android APK

on:
  push:
    branches:
      - alpha
      - beta
      - main

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Grant Execute Permission for Gradlew
        run: chmod +x gradlew

      # --- ALPHA BRANCH BUILD ---
      - name: Build Alpha Debug APK
        if: github.ref == 'refs/heads/alpha'
        run: ./gradlew assembleRelease

      - name: Publish Alpha Pre-Release
        if: github.ref == 'refs/heads/alpha'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: alpha-build-${{ github.run_number }}
          name: "Alpha Build #${{ github.run_number }}"
          body: "Automated alpha testing build generated from `alpha` branch."
          prerelease: true
          files: app/build/outputs/apk/release/app-release.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # --- BETA BRANCH BUILD ---
      - name: Build Beta Debug APK
        if: github.ref == 'refs/heads/beta'
        run: ./gradlew assembleDebug

      - name: Publish Beta Pre-Release
        if: github.ref == 'refs/heads/beta'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: beta-build-${{ github.run_number }}
          name: "Beta Build #${{ github.run_number }}"
          body: "Automated beta testing build generated from `beta` branch."
          prerelease: true
          files: app/build/outputs/apk/debug/app-debug.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # --- MAIN BRANCH BUILD ---
      - name: Build Stable Release APK
        if: github.ref == 'refs/heads/main'
        run: ./gradlew assembleRelease

      - name: Publish Stable Release
        if: github.ref == 'refs/heads/main'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v1.0.${{ github.run_number }}
          name: "Stable Release v1.0.${{ github.run_number }}"
          body: "Official release generated from `main` branch."
          prerelease: false
          files: app/build/outputs/apk/release/app-release.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}